# Data source: "mock" (default, bundled deterministic data) or "supabase"
VITE_DATA_PROVIDER=mock

# Default framework: vsk6a | gsqac | sqaf
VITE_DEFAULT_FRAMEWORK=vsk6a

# Only needed when VITE_DATA_PROVIDER=supabase (see supabase/schema.sql)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
